# total 2 errors

parameters:
    ignoreErrors:
        -
            message: '#^Out of 96 possible return types, only 94 \- 97\.9 %% actually have it\. Add more return types to get over 100 %%$#'
            count: 2
            path: ../src/Rector/AbstractProxyRector.php
