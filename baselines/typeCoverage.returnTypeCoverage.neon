# total 2 errors

parameters:
    ignoreErrors:
        -
            message: '#^Out of 114 possible return types, only 112 \- 98\.2 %% actually have it\. Add more return types to get over 100 %%$#'
            count: 2
            path: ../src/Rector/AbstractProxyRector.php
