# total 2 errors

parameters:
    ignoreErrors:
        -
            message: '#^Out of 91 possible return types, only 89 \- 97\.8 %% actually have it\. Add more return types to get over 100 %%$#'
            count: 2
            path: ../src/Rector/AbstractProxyRector.php
